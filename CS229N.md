# CS229 机器学习课程笔记

> 基于 Stanford CS229 (2022, Andrew Ng & Tengyu Ma) 整理与修正

------

## 1. 监督学习设置与 LMS

### 1.1 基本定义

我们假设存在一个未知的目标函数 $f: \mathcal{X} \to \mathcal{Y}$，学习算法通过训练集 $\{(x^{(i)}, y^{(i)})\}_{i=1}^{m}$ 学习一个假设函数 $h_\theta(x)$ 来近似 $f$。

- $y$ 离散 → **分类 (Classification)**
- $y$ 连续 → **回归 (Regression)**

训练集大小通常记为 $m$，特征维度记为 $n$（或 $d$）。

### 1.2 线性回归与最小二乘

假设函数为线性形式：
$$
h_\theta(x) = \theta^\top x = \sum_{j=0}^{n} \theta_j x_j, \quad x_0 = 1
$$
成本函数（均方误差）：
$$
J(\theta) = \frac{1}{2} \sum_{i=1}^{m} (h_\theta(x^{(i)}) - y^{(i)})^2
$$

### 1.3 梯度下降算法

#### 批量梯度下降 (Batch Gradient Descent)

$$
\theta_j := \theta_j - \alpha \frac{\partial J(\theta)}{\partial \theta_j}
= \theta_j - \alpha \sum_{i=1}^{m} (h_\theta(x^{(i)}) - y^{(i)}) x_j^{(i)}
$$

#### 随机梯度下降 (Stochastic Gradient Descent, SGD)

每次只使用一个样本 $(x^{(i)}, y^{(i)})$：
$$
\theta_j := \theta_j - \alpha (h_\theta(x^{(i)}) - y^{(i)}) x_j^{(i)}
$$
（注意：这是真正的 SGD，原笔记中误写成了批量梯度下降）

#### Mini-Batch 梯度下降

$$
\theta_j := \theta_j - \alpha \frac{1}{|B|} \sum_{i \in B} (h_\theta(x^{(i)}) - y^{(i)}) x_j^{(i)}
$$

其中 $B$ 是一个小批量（batch），大小通常为 16, 32, 64 等。

### 1.4 正规方程 (Normal Equations) – 补充内容

线性回归有解析解：
$$
\theta = (X^\top X)^{-1} X^\top y
$$
其中 $X$ 是设计矩阵（每行为一个样本 $x^{(i)\top}$），$y$ 是标签向量。

------

## 2. 分类与回归的概率视角

### 2.1 线性回归的概率解释

假设：
$$
y^{(i)} = \theta^\top x^{(i)} + \epsilon^{(i)}, \quad \epsilon^{(i)} \sim \mathcal{N}(0, \sigma^2)
$$
则：
$$
y^{(i)} \mid x^{(i)}; \theta \sim \mathcal{N}(\theta^\top x^{(i)}, \sigma^2)
$$
**似然函数**：
$$
L(\theta) = \prod_{i=1}^{m} \frac{1}{\sqrt{2\pi}\sigma} \exp\left(-\frac{(y^{(i)} - \theta^\top x^{(i)})^2}{2\sigma^2}\right)
$$
**对数似然**：
$$
\ell(\theta) = m \log\frac{1}{\sqrt{2\pi}\sigma} - \frac{1}{2\sigma^2} \sum_{i=1}^{m} (y^{(i)} - \theta^\top x^{(i)})^2
$$
最大化 $\ell(\theta)$ 等价于最小化 $\frac{1}{2} \sum (y^{(i)} - \theta^\top x^{(i)})^2$ —— 即最小二乘。

### 2.2 逻辑回归 (Logistic Regression)

对于二分类问题 $y \in \{0,1\}$，使用 **sigmoid 函数**：
$$
h_\theta(x) = g(\theta^\top x) = \frac{1}{1 + e^{-\theta^\top x}}, \quad g(z) = \frac{1}{1+e^{-z}}
$$
解释：
$$
P(y=1 \mid x; \theta) = h_\theta(x), \quad P(y=0 \mid x; \theta) = 1 - h_\theta(x)
$$
**似然函数**：
$$
L(\theta) = \prod_{i=1}^{m} h_\theta(x^{(i)})^{y^{(i)}} (1 - h_\theta(x^{(i)}))^{1-y^{(i)}}
$$
**对数似然**：
$$
\ell(\theta) = \sum_{i=1}^{m} \left[ y^{(i)} \log h_\theta(x^{(i)}) + (1-y^{(i)}) \log(1 - h_\theta(x^{(i)})) \right]
$$
**梯度上升更新规则**（与线性回归的 SGD 形式相同）：
$$
\theta_j := \theta_j + \alpha (y^{(i)} - h_\theta(x^{(i)})) x_j^{(i)}
$$
（注意：逻辑回归的似然是凹函数，因此全局最优解存在。）

### 2.3 牛顿法 (Newton's Method)

#### 用于求根

求解 $f(\theta) = 0$：
$$
\theta^{(t+1)} = \theta^{(t)} - \frac{f(\theta^{(t)})}{f'(\theta^{(t)})}
$$

#### 用于最大化似然

求解 $\ell'(\theta) = 0$：
$$
\theta^{(t+1)} = \theta^{(t)} - \frac{\ell'(\theta^{(t)})}{\ell''(\theta^{(t)})}
$$

#### 多维形式

$$
\theta^{(t+1)} = \theta^{(t)} - H^{-1} \nabla_\theta \ell(\theta)
$$

其中 $H$ 是 Hessian 矩阵：$H_{ij} = \frac{\partial^2 \ell}{\partial \theta_i \partial \theta_j}$。

**特性**：

- 收敛速度快（二次收敛，接近最优点时）
- 无需手动选择学习率
- 高维时计算 Hessian 的逆代价昂贵 $O(n^3)$，不适用于大规模特征
- Hessian 不一定正定，此时更新方向可能不是下降方向

------

## 3. 指数族与广义线性模型 (GLM)

### 3.1 指数族分布 (Exponential Family)

形式定义：
$$
p(y; \eta) = b(y) \exp(\eta^\top T(y) - a(\eta))
$$

- $\eta$：自然参数 (natural parameter)
- $T(y)$：充分统计量（通常取 $T(y)=y$）
- $b(y)$：基础测度 (base measure)
- $a(\eta)$：对数配分函数 (log partition function)

**重要性质**：
$$
\mathbb{E}[T(y); \eta] = \frac{\partial a(\eta)}{\partial \eta}, \quad
\mathrm{Var}[T(y); \eta] = \frac{\partial^2 a(\eta)}{\partial \eta^2}
$$

#### 例子 1：伯努利分布

$$
p(y; \phi) = \phi^y (1-\phi)^{1-y} = \exp\left( y \log\frac{\phi}{1-\phi} + \log(1-\phi) \right)
$$

匹配得：
$$
\eta = \log\frac{\phi}{1-\phi}, \quad T(y)=y, \quad a(\eta)=\log(1+e^\eta)
$$

#### 例子 2：高斯分布（已知方差 $\sigma^2=1$）

$$
p(y; \mu) = \frac{1}{\sqrt{2\pi}} \exp\left(-\frac{(y-\mu)^2}{2}\right)
= \frac{1}{\sqrt{2\pi}} e^{-y^2/2} \exp\left(\mu y - \frac{1}{2}\mu^2\right)
$$

匹配得：
$$
\eta = \mu, \quad T(y)=y, \quad a(\eta)=\frac{1}{2}\eta^2
$$

### 3.2 广义线性模型 (GLM)

**三个设计假设**：

1. **条件分布属于指数族**：$y \mid x; \theta \sim \text{Exponential Family}(\eta)$
2. **自然参数是输入的线性函数**：$\eta = \theta^\top x$
3. **预测时输出条件期望**：$h_\theta(x) = \mathbb{E}[y \mid x; \theta]$ （对 $T(y)=y$ 情况）

#### 常见 GLM 实例

| 数据类型 | 指数族分布 | 链接函数 $g(\eta) = \mathbb{E}[y \mid \eta]$ | 预测函数 $h_\theta(x)$     |
| -------- | ---------- | -------------------------------------------- | -------------------------- |
| 实值     | 高斯       | 恒等 $\eta$                                  | $\theta^\top x$            |
| 二分类   | 伯努利     | logistic $1/(1+e^{-\eta})$                   | $1/(1+e^{-\theta^\top x})$ |
| 计数     | 泊松       | 指数 $e^\eta$                                | $\exp(\theta^\top x)$      |

**学习规则（梯度上升，对所有 GLM 统一）**：
$$
\theta_j := \theta_j + \alpha (y^{(i)} - h_\theta(x^{(i)})) x_j^{(i)}
$$

### 3.3 Softmax 回归（多分类）

对于 $K$ 个类别，标签采用 one-hot 编码。模型输出：
$$
P(y=k \mid x; \theta) = \frac{\exp(\theta_k^\top x)}{\sum_{j=1}^K \exp(\theta_j^\top x)}, \quad k=1,\dots,K
$$
其中 $\theta_k \in \mathbb{R}^{n+1}$ 是第 $k$ 类的参数向量。

**损失函数（交叉熵）**：
$$
J(\theta) = -\frac{1}{m} \sum_{i=1}^{m} \sum_{k=1}^{K} 1\{y^{(i)} = k\} \log \frac{\exp(\theta_k^\top x^{(i)})}{\sum_{j=1}^K \exp(\theta_j^\top x^{(i)})}
$$
或简写为（对单个样本，真实类别为 $y_i$）：
$$
\ell(\theta) = -\theta_{y_i}^\top x^{(i)} + \log \sum_{j=1}^K \exp(\theta_j^\top x^{(i)})
$$
**梯度**（对 $\theta_k$）：
$$
\nabla_{\theta_k} J(\theta) = -\frac{1}{m} \sum_{i=1}^{m} \left[ 1\{y^{(i)}=k\} - P(y^{(i)}=k \mid x^{(i)}; \theta) \right] x^{(i)}
$$

------

## 4. 生成式学习

### 4.1 判别式 vs 生成式

| 特性     | 判别式模型            | 生成式模型                                                   |
| -------- | --------------------- | ------------------------------------------------------------ |
| 建模对象 | $p(y \mid x)$ 直接    | $p(x, y) = p(x \mid y) p(y)$                                 |
| 常见算法 | 逻辑回归、SVM、感知机 | 高斯判别分析、朴素贝叶斯、HMM                                |
| 决策     | 直接分类              | 通过贝叶斯规则：$p(y \mid x) = \frac{p(x \mid y)p(y)}{p(x)}$ |

生成式模型先学习 **类条件分布** $p(x \mid y)$ 和 **类先验** $p(y)$，然后使用贝叶斯定理计算后验概率。

### 4.2 高斯判别分析 (Gaussian Discriminant Analysis, GDA)

**假设**：

- $y \sim \text{Bernoulli}(\phi)$，即 $P(y=1)=\phi, P(y=0)=1-\phi$
- $x \mid y = 0 \sim \mathcal{N}(\mu_0, \Sigma)$，$x \mid y = 1 \sim \mathcal{N}(\mu_1, \Sigma)$（**共享协方差矩阵**）

**参数**：
$$
\phi, \quad \mu_0, \quad \mu_1, \quad \Sigma
$$
**极大似然估计**：
$$
\phi = \frac{1}{m} \sum_{i=1}^{m} 1\{y^{(i)}=1\}, \quad
\mu_0 = \frac{\sum_{i:y^{(i)}=0} x^{(i)}}{\sum_{i=1}^m 1\{y^{(i)}=0\}}, \quad
\mu_1 = \frac{\sum_{i:y^{(i)}=1} x^{(i)}}{\sum_{i=1}^m 1\{y^{(i)}=1\}}
$$

$$
\Sigma = \frac{1}{m} \sum_{i=1}^{m} (x^{(i)} - \mu_{y^{(i)}})(x^{(i)} - \mu_{y^{(i)}})^\top
$$

**预测**：
$$
h_\theta(x) = P(y=1 \mid x) = \frac{1}{1 + e^{-\theta^\top x}} \quad \text{（实际上 GDA 隐含了逻辑回归形式）}
$$
注意：当 GDA 的高斯假设成立时，它比逻辑回归更有效（收敛更快、需要更少数据）；若假设不成立，逻辑回归通常更鲁棒。

### 4.3 朴素贝叶斯 (Naive Bayes)

**核心假设**：在给定类别 $y$ 的条件下，所有特征相互独立：
$$
p(x_1, x_2, \dots, x_n \mid y) = \prod_{j=1}^{n} p(x_j \mid y)
$$

#### 例子：文本分类（垃圾邮件识别）

- 词汇表大小 = $|V|$，特征向量 $x \in \{0,1\}^{|V|}$ 表示单词是否出现（多项伯努利模型）

- 参数：
  $$
  \phi_{j \mid y=1} = P(x_j=1 \mid y=1), \quad \phi_{j \mid y=0} = P(x_j=1 \mid y=0), \quad \phi_y = P(y=1)
  $$

#### 拉普拉斯平滑 (Laplace Smoothing)

为避免概率为零导致乘积为零，对每个特征添加平滑：
$$
P(x_j = v \mid y = k) = \frac{\text{count}(x_j = v, y = k) + 1}{\text{count}(y = k) + |V_j|}
$$
其中 $|V_j|$ 是特征 $x_j$ 可能取值的个数（对于二值特征，$|V_j| = 2$）。

朴素贝叶斯在特征独立性近似成立时非常高效，尤其适用于高维稀疏数据（如文本）。

### 4.4 GDA 与朴素贝叶斯的对比

| 算法       | 特征假设                       | 适用场景                                      |
| ---------- | ------------------------------ | --------------------------------------------- |
| GDA        | 特征向量服从多元高斯（连续值） | 特征连续且近似正态分布，各类别协方差相同      |
| 朴素贝叶斯 | 特征在给定类别下条件独立       | 离散/二值特征（如文本），或特征之间相关性较弱 |

------

## 5. 补充：正则化与模型选择 (略览)

- **过拟合**：模型在训练集上表现很好，但在测试集上表现差。
- **L2 正则化（岭回归）**：$J(\theta) = \frac{1}{2}\sum_{i=1}^{m} (h_\theta(x^{(i)}) - y^{(i)})^2 + \frac{\lambda}{2} \sum_{j=1}^{n} \theta_j^2$
- **L1 正则化（Lasso）**：$J(\theta) = \frac{1}{2}\sum_{i=1}^{m} (h_\theta(x^{(i)}) - y^{(i)})^2 + \lambda \sum_{j=1}^{n} |\theta_j|$（可产生稀疏解）

------

> 本笔记基于斯坦福 CS229 课程 (2022) 内容整理，修正了原笔记中的若干错误（SGD/批量梯度下降混淆、牛顿法公式不准确、指数族性质不完整等），并补充了生成式学习部分。所有行间公式均使用 `$$...$$` 定界符，与 Typora 完美兼容。

|      |      |      |
| ---- | ---- | ---- |
|      |      |      |
|      |      |      |
|      |      |      |
